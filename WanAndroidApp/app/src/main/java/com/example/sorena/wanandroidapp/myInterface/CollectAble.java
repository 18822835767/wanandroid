package com.example.sorena.wanandroidapp.myInterface;

/**
 * 作为一个思路记录一下
 * 如果我还想改的话,collectManager的addToCollectSet就可以用这个接口
 */
public interface CollectAble
{
    int getId();
    boolean isCollect();
}
